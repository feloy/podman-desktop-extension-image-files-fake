{
  "name": "image-filesystem-layers-explorer",
  "displayName": "Image Filesystem Layers Explorer",
  "description": "Explore files in an Image",
  "version": "0.0.1",
  "icon": "icon.png",
  "publisher": "redhat",
  "license": "Apache-2.0",
  "engines": {
    "podman-desktop": "^1.10.3"
  },
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {}
  },
  "scripts": {
    "build": "vite build",
    "test": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest watch --coverage --passWithNoTests",
    "watch": "tsc -w"
  },
  "dependencies": {},
  "devDependencies": {
    "@podman-desktop/api": "^1.10.3",
    "@types/node": "^18",
    "mkdirp": "^3.0.1",
    "typescript": "5.3.3",
    "vite": "^5.0.9",
    "zip-local": "^0.3.5"
  }
}
